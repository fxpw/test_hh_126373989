#!/bin/bash
set -e

if [ ! -d "node_modules" ]; then
  echo "📦 Installing dependencies..."
  bun install
fi

echo "🚀 Starting Bun service on port ${BACKEND_PORT:-3000}"

if [ "$NODE_ENV" = "production" ]; then
  exec bun run app.js
else
  exec bun --watch run app.js
fi