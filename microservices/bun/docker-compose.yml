
services:
  bun_126373989:
    build:
      context: ./docker
      dockerfile: Dockerfile
    container_name: bun_126373989
    env_file:
      - .env
    ports:
      - "${BACKEND_PORT}:3000"
      - "9229:9229"
    networks:
      - test_hh_net_126373989
    volumes:
      - .:/usr/src/app
    entrypoint: ["docker_entrypoint"]

networks:
  test_hh_net_126373989:
    external: true
